import { ChangePasswordDtoTs } from './change-password.dto.ts';

describe('ChangePasswordDtoTs', () => {
  it('should be defined', () => {
    expect(new ChangePasswordDtoTs()).toBeDefined();
  });
});
